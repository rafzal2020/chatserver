#include "protocol.h"

// not yet implemented
//
// read_message()  — reads version|code|len| then exactly len bytes
// send_message()  — formats and writes a complete protocol message
// send_error()    — convenience wrapper for ERR messages
// parse_fields()  — splits body into fields by '|'
